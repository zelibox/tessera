///<reference path="scene.ts"/>
interface IConfig {
    puzzleSize: number,
    rows: number,
    cols: number,
    scene: Scene
}

let config:IConfig = {
    puzzleSize: 20,
    rows: 22,
    cols: 12,
    scene: null
};
